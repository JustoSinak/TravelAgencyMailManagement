# Optional Real-time Communication (install separately if needed)
channels==4.0.0
channels-redis==4.2.0

# Optional GraphQL (install separately if needed)
graphene-django==3.2.0

# Optional Task Queue (install separately if needed)
celery==5.3.4
django-celery-beat==2.5.0
django-celery-results==2.5.1
redis==5.0.1

# Optional Machine Learning (install separately if needed)
scikit-learn==1.3.2
joblib==1.3.2
numpy==1.24.4
nltk==3.8.1

# Development tools
factory-boy==3.3.0
